import productsHistory from "../productsHistory";
import Vue from "vue";
import Vuex from "vuex";

Vue.use(Vuex)

// const store = new Vuex.Store({
//     modules: {productsHistory}
// })

describe('productsHistory', () => {
    it('should have default value an empty object', function () {

    });
})